all:
 vars:
   ansible_user: admin
   ansible_ssh_private_key_file: /home/rdi_aura/TP1_Docker_WSL/id_rsa
 children:
  # groupe de machines de prod
   prod:
     hosts: 
      laura.damas.takima.school:
